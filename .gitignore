# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/
.env
*.log

# --- Backend venv (your demo uses backend/.venv) ---
backend/.venv/

# --- Node / Vite ---
frontend/node_modules/
frontend/dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- OS / Editor ---
.DS_Store
*.swp
.claude/

# --- Backend runtime state (generated, not source) ---
backend/snapshots/
backend/openai_usage.jsonl
backend/audit.log
backend/client_settings.json
backend/ambient_tasks.db
backend/ambient_tasks.db-wal
backend/ambient_tasks.db-shm
backend/ambient_memory.db
backend/ambient_memory.db-wal
backend/ambient_memory.db-shm
# Health-watcher runtime DB — created on first run (was committed by accident)
backend/health_watcher.db
backend/health_watcher.db-wal
backend/health_watcher.db-shm

# --- Docker shipping artifacts (large, built on demand) ---
docker-dist/
xco-mcp-client-*.tar
xco-mcp-client-*.tar.gz

# --- Design references / internal notes — not shipped ---
FIGMA/
tmp/
docs/
MCP_CLIENT_HANDOFF.md
MCP_SERVER_BUG_NOTES.md
MCP_SERVER_PLAN_BUILDER_REQUEST.md


# Top-level node_modules (we already exclude frontend/node_modules; this
# catches accidental top-level installs that some scripts trigger)
/node_modules/

# Stray developer log captures
*.ttt

# --- Secrets / credentials / keys (belt-and-suspenders for a public repo) ---
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.crt
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*_rsa
*_ed25519
*.secret
secrets.*
