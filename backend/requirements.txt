# Backend runtime dependencies — pinned to the tested versions for
# reproducible release images (see RELEASE_CHECKLIST.md / RELEASE_NOTES.md).
# Bump deliberately and re-run the release checks.
fastapi==0.138.1
uvicorn[standard]==0.49.0
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.13.4
lxml==6.1.1
python-docx==1.2.0
